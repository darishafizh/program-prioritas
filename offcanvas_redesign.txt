<!-- OFFCANVAS DETAIL 75% LEBAR LAYAR (Muncul dari kanan saat titik lokasi diklik) -->
    <template x-teleport="body">
    <div x-show="selectedPoint" style="display: none; z-index: 99999;"
        class="fixed inset-0 overflow-hidden pointer-events-none" style="font-family: 'Inter', sans-serif;">
        <div class="fixed inset-0 w-full flex justify-end pointer-events-none">
            <div x-show="selectedPoint" @click.away="closeDetailPanel()"
                style="width: 60vw !important; max-width: 60vw !important; font-family: 'Inter', sans-serif;"
                x-transition:enter="transform transition ease-in-out duration-300 sm:duration-500"
                x-transition:enter-start="translate-x-full" x-transition:enter-end="translate-x-0"
                x-transition:leave="transform transition ease-in-out duration-300 sm:duration-500"
                x-transition:leave-start="translate-x-0" x-transition:leave-end="translate-x-full"
                class="h-full bg-white dark:bg-gray-900 shadow-2xl flex flex-col text-left pointer-events-auto border-l border-gray-200/80 dark:border-gray-800 overflow-hidden">

                <!-- HEADER -->
                <div class="px-5 py-3 border-b border-gray-100 dark:border-gray-800 flex justify-between items-center flex-shrink-0 bg-white dark:bg-gray-900">
                    <div class="flex items-center gap-3 min-w-0">
                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold uppercase tracking-wider shrink-0"
                            :class="selectedPoint?.tahap === 'serah_terima' ?
                                'bg-emerald-50 text-emerald-600 dark:bg-emerald-500/10 dark:text-emerald-400' :
                                'bg-amber-50 text-amber-600 dark:bg-amber-500/10 dark:text-amber-400'"
                            x-text="selectedPoint?.tahap_label"></span>
                        <div class="min-w-0">
                            <h3 class="text-sm font-semibold text-gray-900 dark:text-white truncate"
                                x-text="selectedPoint?.nama"></h3>
                            <p class="text-[11px] text-gray-400 dark:text-gray-500 truncate" 
                                x-text="[selectedPoint?.desa, selectedPoint?.kecamatan, selectedPoint?.kabupaten, selectedPoint?.provinsi].filter(Boolean).join(', ') || '-'"></p>
                        </div>
                    </div>
                    <button type="button" @click="closeDetailPanel()"
                        class="cursor-pointer text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 p-1 transition-colors focus:outline-none rounded-md hover:bg-gray-50 dark:hover:bg-gray-800 shrink-0"
                        title="Tutup Panel">
                        <i class="fa-solid fa-xmark text-sm"></i>
                    </button>
                </div>

                <!-- BODY -->
                <div class="flex-1 min-h-0 flex flex-col overflow-hidden bg-white dark:bg-gray-900 px-5 py-4">
                    <template x-if="selectedPoint">
                        <div class="flex flex-col h-full gap-4">
                        
                            <!-- ==================== KONSTRUKSI ==================== -->
                            <template x-if="selectedPoint?.tahap === 'konstruksi'">
                                <div class="flex flex-col h-full gap-4">
                                    
                                    <!-- ROW 1: Profil + Dokumentasi side by side -->
                                    <div class="grid grid-cols-5 gap-6 shrink-0">
                                        
                                        <!-- Profil KNMP (2 cols) -->
                                        <div class="col-span-2">
                                            <div class="text-[11px] font-semibold text-gray-400 uppercase tracking-wider mb-3">Profil KNMP</div>
                                            <div class="space-y-2.5">
                                                <div class="flex items-baseline justify-between">
                                                    <span class="text-[11px] text-gray-400">Jumlah Nelayan</span>
                                                    <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300">1.250 Orang</span>
                                                </div>
                                                <div class="flex items-baseline justify-between">
                                                    <span class="text-[11px] text-gray-400">Jumlah Kapal</span>
                                                    <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300">320 Unit</span>
                                                </div>
                                                <div class="flex items-baseline justify-between">
                                                    <span class="text-[11px] text-gray-400">Pendapatan Rata-rata</span>
                                                    <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300">Rp 3.5 Juta/Bln</span>
                                                </div>
                                                <div class="flex items-baseline justify-between">
                                                    <span class="text-[11px] text-gray-400">Volume Tangkapan</span>
                                                    <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300">450 Ton/Bln</span>
                                                </div>
                                                <div class="pt-1 border-t border-gray-100 dark:border-gray-800">
                                                    <span class="text-[10px] text-gray-400 uppercase tracking-wider">Kontraktor</span>
                                                    <div class="text-[11px] font-medium text-gray-700 dark:text-gray-300 truncate mt-0.5" x-text="selectedPoint?.kontraktor || '-'"></div>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Dokumentasi Fisik (3 cols) -->
                                        <div class="col-span-3">
                                            <div class="text-[11px] font-semibold text-gray-400 uppercase tracking-wider mb-3">Dokumentasi Fisik</div>
                                            <div class="grid grid-cols-2 gap-4">
                                                <!-- Before -->
                                                <div>
                                                    <div class="text-[10px] text-gray-400 uppercase tracking-wider mb-2">Before (0%)</div>
                                                    <div class="grid grid-cols-3 gap-1.5">
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[0]">
                                                                <img :src="selectedPoint.fotosBefore[0].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[0]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[1]">
                                                                <img :src="selectedPoint.fotosBefore[1].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[1]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[2]">
                                                                <img :src="selectedPoint.fotosBefore[2].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[2]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                    </div>
                                                </div>
                                                <!-- Progres -->
                                                <div>
                                                    <div class="text-[10px] text-gray-400 uppercase tracking-wider mb-2">Progres</div>
                                                    <div class="grid grid-cols-3 gap-1.5">
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[0]">
                                                                <img :src="selectedPoint.fotosAfter[0].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[0]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[1]">
                                                                <img :src="selectedPoint.fotosAfter[1].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[1]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                        <div class="aspect-square bg-gray-100 dark:bg-gray-800 rounded-md overflow-hidden flex items-center justify-center">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[2]">
                                                                <img :src="selectedPoint.fotosAfter[2].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[2]">
                                                                <i class="fa-regular fa-image text-gray-300 text-xs"></i>
                                                            </template>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Divider -->
                                    <div class="border-t border-gray-100 dark:border-gray-800 shrink-0"></div>

                                    <!-- ROW 2: Kurva S -->
                                    <div x-data="{
                                        activePoint: null,
                                        chartW: 380, chartH: 100, padL: 5, padT: 5,
                                        get kurva() {
                                            const arr = selectedPoint?.kurvaS || [];
                                            const n = Math.max(1, arr.length - 1);
                                            return arr.map((k, idx) => ({
                                                ...k, idx,
                                                minggu: k.minggu || ('Mg ' + (idx+1)),
                                                x: this.padL + (idx / n) * this.chartW,
                                                yRencana: k.rencana != null ? this.padT + (1 - k.rencana / 100) * this.chartH : null,
                                                yRealisasi: k.realisasi != null ? this.padT + (1 - k.realisasi / 100) * this.chartH : null
                                            }));
                                        },
                                        getRencanaPath() {
                                            return this.kurva.filter(k=>k.yRencana!=null).map((k,i)=>${i?'L':'M'}  ).join(' ');
                                        },
                                        getRealisasiPath() {
                                            return this.kurva.filter(k=>k.yRealisasi!=null).map((k,i)=>${i?'L':'M'}  ).join(' ');
                                        },
                                        getFillPath() {
                                            const v = this.kurva.filter(k=>k.yRealisasi!=null);
                                            if(!v.length) return '';
                                            let d=M  ;
                                            v.forEach(p=>d+= L  );
                                            return d+ L   Z;
                                        }
                                    }"
                                        x-init="('selectedPoint', () => { activePoint = null; })"
                                        class="flex flex-col flex-1 min-h-0">
                                        
                                        <!-- Chart Header -->
                                        <div class="flex justify-between items-center mb-2 shrink-0">
                                            <div class="text-[11px] font-semibold text-gray-400 uppercase tracking-wider">Kurva S</div>
                                            <div class="flex items-center gap-4 text-[10px] text-gray-400">
                                                <div class="flex items-center gap-1.5">
                                                    <svg width="16" height="2"><line x1="0" y1="1" x2="16" y2="1" stroke="#9ca3af" stroke-width="1.5" stroke-dasharray="3,2"/></svg>
                                                    <span>Rencana</span>
                                                </div>
                                                <div class="flex items-center gap-1.5">
                                                    <svg width="16" height="2"><line x1="0" y1="1" x2="16" y2="1" stroke="#374151" stroke-width="2"/></svg>
                                                    <span>Realisasi</span>
                                                </div>
                                                <template x-if="activePoint">
                                                    <div class="text-gray-600 font-medium ml-2">
                                                        <span x-text="activePoint.minggu"></span>
                                                        &mdash; R: <span x-text="formatDec(activePoint.rencana)+'%'"></span>,
                                                        A: <span x-text="activePoint.realisasi != null ? formatDec(activePoint.realisasi)+'%' : '-'"></span>
                                                    </div>
                                                </template>
                                            </div>
                                        </div>

                                        <!-- Chart Body -->
                                        <div class="flex flex-1 min-h-0 gap-1">
                                            <!-- Y-axis labels -->
                                            <div class="flex flex-col justify-between text-[10px] text-gray-300 dark:text-gray-600 shrink-0 py-[5px]" style="width:24px;">
                                                <span class="text-right">100</span>
                                                <span class="text-right">50</span>
                                                <span class="text-right">0</span>
                                            </div>
                                            <!-- SVG Chart -->
                                            <div class="flex-1 min-w-0 relative">
                                                <svg class="w-full h-full" viewBox="0 0 390 115" preserveAspectRatio="none">
                                                    <!-- Grid -->
                                                    <line x1="5" y1="5" x2="385" y2="5" stroke="#f3f4f6" stroke-width="1"/>
                                                    <line x1="5" y1="55" x2="385" y2="55" stroke="#f3f4f6" stroke-width="1" stroke-dasharray="3,3"/>
                                                    <line x1="5" y1="105" x2="385" y2="105" stroke="#e5e7eb" stroke-width="1"/>

                                                    <!-- Hover line -->
                                                    <template x-if="activePoint">
                                                        <line :x1="activePoint.x" y1="5" :x2="activePoint.x" y2="105" stroke="#d1d5db" stroke-width="1" stroke-dasharray="2,2"/>
                                                    </template>

                                                    <!-- Rencana dashed -->
                                                    <path :d="getRencanaPath()" fill="none" stroke="#9ca3af" stroke-width="1.5" stroke-dasharray="5,3"/>
                                                    
                                                    <!-- Realisasi solid + fill -->
                                                    <path :d="getFillPath()" fill="rgba(55,65,81,0.06)"/>
                                                    <path :d="getRealisasiPath()" fill="none" stroke="#374151" stroke-width="2"/>

                                                    <!-- Points -->
                                                    <template x-for="(pt, idx) in kurva" :key="idx">
                                                        <g class="cursor-pointer" @mouseenter="activePoint = pt" @mouseleave="activePoint = null">
                                                            <rect :x="pt.x - 12" y="5" width="24" height="100" fill="transparent"/>
                                                            <template x-if="pt.yRencana != null">
                                                                <circle :cx="pt.x" :cy="pt.yRencana" r="2" fill="#9ca3af"/>
                                                            </template>
                                                            <template x-if="pt.yRealisasi != null">
                                                                <circle :cx="pt.x" :cy="pt.yRealisasi" r="3" fill="#374151" stroke="white" stroke-width="1.5"/>
                                                            </template>
                                                        </g>
                                                    </template>
                                                </svg>
                                            </div>
                                        </div>
                                        <!-- X-axis labels -->
                                        <div class="flex ml-[25px] shrink-0 mt-1">
                                            <template x-for="(pt, idx) in kurva" :key="idx">
                                                <template x-if="kurva.length <= 12 || idx % Math.ceil(kurva.length/8) === 0 || idx === kurva.length - 1">
                                                    <span class="text-[9px] text-gray-300 dark:text-gray-600" 
                                                        :style="position:absolute; left: calc(25px + % * (1 - 25px / 100%))"
                                                        x-text="pt.minggu"></span>
                                                </template>
                                            </template>
                                        </div>
                                    </div>

                                    <!-- ROW 3: Alert Kendala -->
                                    <template x-if="selectedPoint?.deviasi < 0">
                                        <div class="bg-red-50 dark:bg-red-900/10 border border-red-100 dark:border-red-900/20 rounded-lg px-3 py-2.5 flex items-center gap-2.5 shrink-0">
                                            <i class="fa-solid fa-triangle-exclamation text-red-400 text-[11px]"></i>
                                            <p class="text-[11px] text-red-600/80 dark:text-red-400/80 leading-snug">
                                                <span class="font-semibold">Deviasi <span x-text="formatDec(selectedPoint.deviasi) + '%'"></span></span> &mdash; 
                                                Realisasi <span x-text="formatDec(selectedPoint.progres) + '%'"></span> vs Rencana <span x-text="formatDec(selectedPoint.progres_rencana) + '%'"></span>
                                            </p>
                                        </div>
                                    </template>
                                </div>
                            </template>

                            <!-- ==================== SERAH TERIMA ==================== -->
                            <template x-if="selectedPoint?.tahap === 'serah_terima'">
                                <div class="flex flex-col gap-4 w-full">
                                    <div class="text-[11px] font-semibold text-gray-400 uppercase tracking-wider">Data Operasional & BAST</div>
                                    <div class="space-y-2.5">
                                        <div class="flex items-baseline justify-between">
                                            <span class="text-[11px] text-gray-400">Nomor Kontrak</span>
                                            <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300" x-text="selectedPoint?.tahapSerahTerima?.nomor_kontrak || '-'"></span>
                                        </div>
                                        <div class="flex items-baseline justify-between">
                                            <span class="text-[11px] text-gray-400">Tanggal Serah Terima</span>
                                            <span class="text-[11px] font-medium text-gray-700 dark:text-gray-300" x-text="selectedPoint?.tahapSerahTerima?.tanggal_serah || selectedPoint?.tahapSerahTerima?.tanggal || '-'"></span>
                                        </div>
                                        <div class="flex items-baseline justify-between">
                                            <span class="text-[11px] text-gray-400">Status Sarpras</span>
                                            <span class="text-[11px] font-medium text-emerald-600">Beroperasi Penuh</span>
                                        </div>
                                    </div>
                                    <div class="border-t border-gray-100 dark:border-gray-800 my-1"></div>
                                    <div class="flex items-start gap-2 text-[11px] text-gray-400 leading-relaxed">
                                        <i class="fa-solid fa-circle-check mt-0.5 text-gray-300"></i>
                                        <p>Lokasi ini telah diserahterimakan dan saat ini dalam fase operasional pemanfaatan.</p>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </template>
                </div>
            </div>
        </div>
    </div>
    </template>