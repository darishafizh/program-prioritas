<!-- OFFCANVAS DETAIL 75% LEBAR LAYAR (Muncul dari kanan saat titik lokasi diklik) -->
    <template x-teleport="body">
    <div x-show="selectedPoint" style="display: none; z-index: 99999;"
        class="fixed inset-0 overflow-hidden font-sans pointer-events-none">
        <div class="fixed inset-0 w-full flex justify-end pointer-events-none">
            <!-- Offcanvas Panel kanan 60% layar - Fixed Layar Tanpa Scroll -->
            <div x-show="selectedPoint" @click.away="closeDetailPanel()"
                style="width: 60vw !important; max-width: 60vw !important;"
                x-transition:enter="transform transition ease-in-out duration-300 sm:duration-500"
                x-transition:enter-start="translate-x-full" x-transition:enter-end="translate-x-0"
                x-transition:leave="transform transition ease-in-out duration-300 sm:duration-500"
                x-transition:leave-start="translate-x-0" x-transition:leave-end="translate-x-full"
                class="w-3/5 h-full bg-white dark:bg-gray-900 shadow-2xl flex flex-col text-left pointer-events-auto border-l border-gray-200/80 dark:border-gray-800 overflow-hidden">

                <!-- Header -->
                <div class="px-6 py-3 border-b border-gray-100 dark:border-gray-800 flex justify-between items-start flex-shrink-0 bg-white dark:bg-gray-900 shadow-sm gap-4">
                    <div class="flex flex-col items-start gap-1 min-w-0">
                        <div class="flex items-center gap-2 mb-0.5">
                            <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider shrink-0"
                                :class="selectedPoint?.tahap === 'serah_terima' ?
                                    'bg-success/10 dark:bg-success/20 text-success dark:text-emerald-400 border border-success/20' :
                                    'bg-warning/10 dark:bg-amber-400/10 text-warning dark:text-amber-400 border border-warning/20'"
                                x-text="selectedPoint?.tahap_label"></span>
                        </div>
                        <h3 class="text-[15px] font-semibold tracking-tight text-gray-800 dark:text-white leading-snug truncate w-full"
                            x-text="selectedPoint?.nama"></h3>
                        <p class="text-[11px] text-gray-500 dark:text-gray-400 truncate w-full" 
                            x-text="[selectedPoint?.desa, selectedPoint?.kecamatan, selectedPoint?.kabupaten, selectedPoint?.provinsi].filter(Boolean).join(', ') || '-'"></p>
                    </div>

                    <div class="flex items-center shrink-0">
                        <button type="button" @click="closeDetailPanel()"
                            class="cursor-pointer text-gray-400 hover:text-gray-700 dark:hover:text-gray-200 p-1.5 transition-colors focus:outline-none rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800"
                            title="Tutup Panel">
                            <i class="fa-solid fa-xmark text-lg"></i>
                        </button>
                    </div>
                </div>

                <!-- Body (Fit to screen, no scroll ideally) -->
                <div class="flex-1 min-h-0 flex flex-col overflow-hidden bg-gray-50/60 dark:bg-gray-950/50 p-4 gap-3">
                    <template x-if="selectedPoint">
                        <div class="flex flex-col gap-3 w-full h-full">
                        
                            <!-- KONSTRUKSI LAYOUT -->
                            <template x-if="selectedPoint?.tahap === 'konstruksi'">
                                <div class="flex flex-col gap-3 w-full h-full">
                                    
                                    <!-- Baris 1: Profil KNMP & 6 Gambar -->
                                    <div class="grid grid-cols-2 gap-4 shrink-0 h-[32%] min-h-[140px]">
                                        <!-- Kolom Kiri: Profil KNMP -->
                                        <div class="bg-transparent p-0 flex flex-col h-full">
                                            <h4 class="text-sm font-bold flex items-center gap-2 mb-2 text-gray-800 dark:text-gray-200 shrink-0">
                                                Profil KNMP
                                            </h4>
                                            <div class="grid grid-cols-2 gap-2 flex-1">
                                                <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-2 flex flex-col justify-center">
                                                    <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-0.5">Jumlah Nelayan</div>
                                                    <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200">1.250 Orang</div>
                                                </div>
                                                <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-2 flex flex-col justify-center">
                                                    <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-0.5">Jumlah Kapal</div>
                                                    <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200">320 Unit</div>
                                                </div>
                                                <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-2 flex flex-col justify-center">
                                                    <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-0.5">Pendapatan Rata-rata</div>
                                                    <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200">Rp 3.5 Juta/Bln</div>
                                                </div>
                                                <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-2 flex flex-col justify-center">
                                                    <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-0.5">Volume Tangkapan</div>
                                                    <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200">450 Ton/Bln</div>
                                                </div>
                                                <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-2 col-span-2 flex items-center justify-between">
                                                    <div>
                                                        <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-0.5">Kontraktor Pelaksana</div>
                                                        <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200 truncate" x-text="selectedPoint?.kontraktor || 'PT. Nusantara Bangun'"></div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Kolom Kanan: 6 Gambar (3 Before, 3 Progres) -->
                                        <div class="bg-transparent p-0 flex flex-col h-full">
                                            <h4 class="text-sm font-bold flex items-center gap-2 mb-2 text-gray-800 dark:text-gray-200 shrink-0">
                                                Dokumentasi Fisik
                                            </h4>
                                            <div class="grid grid-cols-2 gap-3 flex-1 h-full min-h-0">
                                                <!-- 3 Gambar Before -->
                                                <div class="flex flex-col gap-1 h-full">
                                                    <div class="text-[10px] font-semibold text-gray-500 uppercase tracking-wider mb-0.5 shrink-0">Before (0%)</div>
                                                    <div class="grid grid-rows-3 gap-1 h-full min-h-0">
                                                        <div class="bg-gray-100 dark:bg-gray-800 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[0]">
                                                                <img :src="selectedPoint.fotosBefore[0].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[0]">
                                                                <i class="fa-regular fa-image text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                        <div class="bg-gray-100 dark:bg-gray-800 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[1]">
                                                                <img :src="selectedPoint.fotosBefore[1].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[1]">
                                                                <i class="fa-regular fa-image text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                        <div class="bg-gray-100 dark:bg-gray-800 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosBefore && selectedPoint.fotosBefore[2]">
                                                                <img :src="selectedPoint.fotosBefore[2].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosBefore || !selectedPoint.fotosBefore[2]">
                                                                <i class="fa-regular fa-image text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                    </div>
                                                </div>
                                                
                                                <!-- 3 Gambar Progres -->
                                                <div class="flex flex-col gap-1 h-full">
                                                    <div class="text-[10px] font-semibold text-gray-500 uppercase tracking-wider mb-0.5 shrink-0">Progres Current</div>
                                                    <div class="grid grid-rows-3 gap-1 h-full min-h-0">
                                                        <div class="bg-gray-50 dark:bg-gray-900/30 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[0]">
                                                                <img :src="selectedPoint.fotosAfter[0].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[0]">
                                                                <i class="fa-solid fa-camera text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                        <div class="bg-gray-50 dark:bg-gray-900/30 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[1]">
                                                                <img :src="selectedPoint.fotosAfter[1].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[1]">
                                                                <i class="fa-solid fa-camera text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                        <div class="bg-gray-50 dark:bg-gray-900/30 rounded overflow-hidden relative border border-gray-200 dark:border-gray-700 flex items-center justify-center h-full">
                                                            <template x-if="selectedPoint?.fotosAfter && selectedPoint.fotosAfter[2]">
                                                                <img :src="selectedPoint.fotosAfter[2].url" class="w-full h-full object-cover">
                                                            </template>
                                                            <template x-if="!selectedPoint?.fotosAfter || !selectedPoint.fotosAfter[2]">
                                                                <i class="fa-solid fa-camera text-gray-400 text-[10px]"></i>
                                                            </template>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Baris 2: Kurva S -->
                                    <div x-data="{
                                        activePoint: null,
                                        get kurva() {
                                            const arr = selectedPoint?.kurvaS || [];
                                            const n = Math.max(1, arr.length - 1);
                                            return arr.map((k, idx) => ({
                                                ...k,
                                                idx,
                                                minggu: k.minggu || ('Mg-' + (idx+1)),
                                                x: 40 + (idx / n) * 350,
                                                yRencana: k.rencana !== null && k.rencana !== undefined ? 95 - (k.rencana / 100) * 85 : null,
                                                yRealisasi: k.realisasi !== null && k.realisasi !== undefined ? 95 - (k.realisasi / 100) * 85 : null
                                            }));
                                        },
                                        getRencanaPath() {
                                            if (!this.kurva.length) return '';
                                            return this.kurva.map((k, i) => ${i === 0 ? 'M' : 'L'}  ).join(' ');
                                        },
                                        getRealisasiPath() {
                                            const valid = this.kurva.filter(k => k.yRealisasi !== null);
                                            if (!valid.length) return '';
                                            return valid.map((k, i) => ${i === 0 ? 'M' : 'L'}  ).join(' ');
                                        },
                                        getFillPath() {
                                            const valid = this.kurva.filter(k => k.yRealisasi !== null);
                                            if (!valid.length) return '';
                                            let path = M  95;
                                            valid.forEach(p => path +=  L  );
                                            path +=  L  95 Z;
                                            return path;
                                        }
                                    }"
                                        x-init="$watch('selectedPoint', () => { activePoint = null; })"
                                        class="w-full bg-transparent p-0 flex flex-col flex-1 min-h-[160px] font-sans">
                                        
                                        <div class="flex justify-between items-center mb-1 shrink-0">
                                            <h4 class="text-sm font-bold flex items-center gap-2 text-gray-800 dark:text-gray-200">
                                                Kurva S (Kinerja dan Fisik Konstruksi)
                                            </h4>
                                            
                                            <!-- Legend / Values -->
                                            <div class="flex items-center gap-3 text-[10px]">
                                                <div class="flex items-center gap-1.5">
                                                    <span class="w-3 h-0.5 border-b border-dashed border-gray-400 block"></span>
                                                    <span class="text-gray-500">Rencana: <span class="font-semibold text-gray-700 dark:text-gray-300" x-text="activePoint ? formatDec(activePoint.rencana) + '%' : '-'"></span></span>
                                                </div>
                                                <div class="flex items-center gap-1.5">
                                                    <span class="w-3 h-0.5 bg-gray-600 block"></span>
                                                    <span class="text-gray-500">Realisasi: <span class="font-semibold text-gray-700 dark:text-gray-300" x-text="activePoint && activePoint.realisasi !== null ? formatDec(activePoint.realisasi) + '%' : '-'"></span></span>
                                                </div>
                                                <template x-if="activePoint">
                                                    <div class="ml-2 px-1.5 py-0.5 bg-gray-200 dark:bg-gray-700 rounded text-gray-700 dark:text-gray-200 font-semibold">
                                                        <span x-text="activePoint.label || activePoint.minggu"></span>
                                                    </div>
                                                </template>
                                            </div>
                                        </div>

                                        <template x-if="(selectedPoint?.progres || 0) >= 0">
                                            <div class="w-full relative flex items-center justify-center select-none flex-1">
                                                <svg class="w-full h-full overflow-visible" viewBox="0 0 400 110" preserveAspectRatio="none">
                                                    <!-- Y Axis Labels -->
                                                    <text x="32" y="14" text-anchor="end" fill="currentColor" class="text-[8px] font-medium text-gray-400 dark:text-gray-500">100%</text>
                                                    <text x="32" y="56" text-anchor="end" fill="currentColor" class="text-[8px] font-medium text-gray-400 dark:text-gray-500">50%</text>
                                                    <text x="32" y="98" text-anchor="end" fill="currentColor" class="text-[8px] font-medium text-gray-400 dark:text-gray-500">0%</text>

                                                    <!-- Grid Lines -->
                                                    <line x1="38" y1="10" x2="395" y2="10" stroke="currentColor" class="text-gray-200 dark:text-gray-800" stroke-width="1" stroke-dasharray="2,2" />
                                                    <line x1="38" y1="52.5" x2="395" y2="52.5" stroke="currentColor" class="text-gray-200 dark:text-gray-800" stroke-width="1" stroke-dasharray="2,2" />
                                                    <line x1="38" y1="95" x2="395" y2="95" stroke="currentColor" class="text-gray-300 dark:text-gray-700" stroke-width="1" />
                                                    
                                                    <!-- Y Axis Line -->
                                                    <line x1="38" y1="5" x2="38" y2="95" stroke="currentColor" class="text-gray-300 dark:text-gray-700" stroke-width="1" />
                                                    <text x="15" y="50" transform="rotate(-90 15,50)" text-anchor="middle" fill="currentColor" class="text-[8px] font-semibold text-gray-400 tracking-wider">NILAI (%)</text>
                                                    <text x="216" y="108" text-anchor="middle" fill="currentColor" class="text-[8px] font-semibold text-gray-400 tracking-wider">MINGGU</text>

                                                    <template x-if="activePoint !== null">
                                                        <line :x1="activePoint.x" y1="10" :x2="activePoint.x" y2="95" stroke="#4b5563" stroke-width="1" stroke-dasharray="3,3" opacity="0.4" />
                                                    </template>

                                                    <!-- X Axis Marks / Labels (Show sparsely if too many) -->
                                                    <template x-for="(pt, idx) in kurva" :key="idx">
                                                        <g>
                                                            <line :x1="pt.x" y1="95" :x2="pt.x" y2="98" stroke="currentColor" class="text-gray-300 dark:text-gray-700" stroke-width="1" />
                                                            <template x-if="kurva.length <= 12 || idx % Math.ceil(kurva.length/8) === 0 || idx === kurva.length - 1">
                                                                <text :x="pt.x" y="105" text-anchor="middle" fill="currentColor" class="text-[7px] text-gray-400" x-text="pt.minggu"></text>
                                                            </template>
                                                        </g>
                                                    </template>

                                                    <!-- Rencana (Dashed Line) -->
                                                    <path :d="getRencanaPath()" fill="none" stroke="#9ca3af" stroke-width="1.5" stroke-dasharray="4,3" />
                                                    
                                                    <!-- Realisasi (Solid Line + Fill) -->
                                                    <path :d="getFillPath()" fill="rgba(75, 85, 99, 0.1)" />
                                                    <path :d="getRealisasiPath()" fill="none" stroke="#4b5563" stroke-width="2" />

                                                    <!-- Data Points -->
                                                    <template x-for="(pt, idx) in kurva" :key="idx">
                                                        <g class="cursor-pointer" @mouseenter="activePoint = pt" @click="activePoint = pt">
                                                            <circle :cx="pt.x" :cy="pt.yRencana" r="2" class="fill-gray-400 transition-all" />
                                                            <template x-if="pt.realisasi !== null && pt.realisasi !== undefined">
                                                                <circle :cx="pt.x" :cy="pt.yRealisasi" r="3" class="fill-gray-600 dark:fill-gray-400 transition-all stroke-white dark:stroke-gray-900" stroke-width="1" />
                                                            </template>
                                                            <!-- Invisible hover catch area -->
                                                            <rect :x="pt.x - 10" y="10" width="20" height="85" fill="transparent" />
                                                        </g>
                                                    </template>
                                                </svg>
                                            </div>
                                        </template>
                                    </div>

                                    <!-- Baris 3: Alert Kendala -->
                                    <template x-if="selectedPoint?.deviasi < 0">
                                        <div class="bg-danger/10 border border-danger/30 rounded-xl p-3 flex items-start gap-3 w-full shrink-0">
                                            <div class="w-8 h-8 rounded-full bg-danger/20 text-danger flex items-center justify-center shrink-0">
                                                <i class="fa-solid fa-triangle-exclamation text-sm"></i>
                                            </div>
                                            <div class="flex-1 min-w-0">
                                                <h4 class="text-[11px] font-bold text-danger mb-0.5">Catatan Kendala & Deviasi Negatif</h4>
                                                <p class="text-[10px] text-danger/90 leading-snug">
                                                    Deviasi <strong x-text="formatDec(selectedPoint.deviasi) + '%'"></strong> (Realisasi: <span x-text="formatDec(selectedPoint.progres) + '%'"></span> - Rencana: <span x-text="formatDec(selectedPoint.progres_rencana) + '%'"></span>). Diperlukan intervensi segera pada aspek teknis/manajerial untuk mengejar ketertinggalan jadwal konstruksi.
                                                </p>
                                            </div>
                                        </div>
                                    </template>
                                </div>
                            </template>

                            <!-- SERAH TERIMA SPECIFIC -->
                            <template x-if="selectedPoint?.tahap === 'serah_terima'">
                                <div class="flex flex-col gap-3 w-full h-full">
                                    <div class="bg-transparent p-0 flex flex-col h-full">
                                        <h4 class="text-sm font-bold flex items-center gap-2 mb-3 text-gray-800 dark:text-gray-200">
                                            Data Operasional & BAST
                                        </h4>
                                        
                                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 mb-3">
                                            <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-3">
                                                <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-1">Nomor Kontrak BAST</div>
                                                <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200 truncate" x-text="selectedPoint?.tahapSerahTerima?.nomor_kontrak || 'BAST-2026/01'"></div>
                                            </div>
                                            <div class="bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-lg p-3">
                                                <div class="text-[10px] text-gray-500 uppercase tracking-wider mb-1">Tanggal Serah Terima</div>
                                                <div class="text-[11px] font-semibold text-gray-800 dark:text-gray-200 truncate" x-text="selectedPoint?.tahapSerahTerima?.tanggal_serah || selectedPoint?.tahapSerahTerima?.tanggal || '-'"></div>
                                            </div>
                                            <div class="bg-success/10 border border-success/20 rounded-lg p-3">
                                                <div class="text-[10px] text-success uppercase tracking-wider mb-1">Status Sarpras</div>
                                                <div class="text-[11px] font-semibold text-success truncate">Beroperasi Penuh</div>
                                            </div>
                                        </div>

                                        <div class="flex items-start gap-3 p-3 bg-gray-100 dark:bg-gray-800/80 border border-gray-200 dark:border-gray-700 rounded-xl text-xs text-gray-700 dark:text-gray-300">
                                            <i class="fa-solid fa-circle-check mt-0.5 text-success"></i>
                                            <p>Lokasi ini telah diserahterimakan dan saat ini dalam fase operasional pemanfaatan. Fasilitas KNMP diharapkan dapat memberikan peningkatan kesejahteraan secara terukur kepada komunitas nelayan setempat.</p>
                                        </div>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </template>
                </div>
            </div>
        </div>
    </div>
    </template>